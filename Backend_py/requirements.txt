# --- Core Framework ---
fastapi>=0.100.0
uvicorn[standard]>=0.20.0   # The server that runs the app
pydantic>=2.0.0             # For data validation (Product models)

# --- Database ---
mysql-connector-python>=8.0.0 # To connect to your MySQL database

# --- Caching ---
redis>=5.0.0                # To connect to your Redis cache

# --- Security & Authentication (MFA/Login) ---
python-multipart            # Required for FastAPI Form data (Login)
passlib[bcrypt]             # For hashing passwords (security best practice)
python-jose[cryptography]   # For generating JWT Tokens (Login sessions)

# --- Utilities ---
python-dotenv               # To load variables from .env file (DB passwords)
requests                    # To make external API calls (e.g., Google OAuth)
email-validator             # To validate email formats in Pydantic